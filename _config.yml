# Site settings
title: "#mettWiki"
email: mett@ircmett.de
description: > # this means to ignore newlines until "baseurl:"
  Der rohe, fleischige Teil des Internets
baseurl: "" # the subpath of your site, e.g. /blog/
url: "http://ircmett.de" # the base hostname & protocol for your site

# Build settings
markdown: kramdown

defaults:
  -
    scope:
      path: "pages"
      type: "pages" # previously `page` in Jekyll 2.2.
    values:
      layout: "default"
